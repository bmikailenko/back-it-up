Ben Mikailenko
Back it up!

Program backs up files into local ".backup" folder. Restores files with "-r" command.

To make:

    make

To clean:

    make clean

To run:

    ./main      (to backup)
    ./main -r   (to restore)